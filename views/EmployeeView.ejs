<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Pracownik - Szczegóły</title>
</head>
<body>
<header style="background-color: #f4f4f4; padding: 10px;">
    <nav style="display: flex; justify-content: space-between; align-items: center;">
        <div>
            <form action="/logout" method="POST" style="display: inline;">
                <button type="submit" style="background-color: #ff4d4d; color: white; border: none; padding: 10px 20px; cursor: pointer;">Wyloguj</button>
            </form>
        </div>
    </nav>
</header>
<h1>Szczegóły pracownika: <%= worker.name %></h1>

<section>
    <h2>Grupy, do których należysz:</h2>
    <% if (groups.length > 0) { %>
        <ul>
            <% groups.forEach(group => { %>
                <li>
                    <p><strong>Nazwa grupy:</strong> <%= group.name %></p>
                    <p><strong>Rola grupy:</strong> <%= group.role %></p>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>Pracownik nie należy do żadnej grupy.</p>
    <% } %>
</section>

</body>
</html>
